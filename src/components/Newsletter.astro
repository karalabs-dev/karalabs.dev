<div class="newsletter">
  <script src="https://f.convertkit.com/ckjs/ck.5.js"></script>
  <form
    action="https://app.kit.com/forms/9238789/subscriptions"
    method="post"
    data-sv-form="9238789"
    data-uid="9b34dfb969"
    data-format="inline"
    data-version="5"
    class="nl-form"
  >
    <h3>Stay in the loop</h3>
    <p>New posts on AI agents, benchmarks, and production systems — no spam.</p>
    <div class="nl-fields">
      <input type="email" name="email_address" placeholder="you@email.com" required />
      <button type="submit">Subscribe</button>
    </div>
  </form>
</div>

<style>
  .newsletter {
    margin-top: 4rem;
    padding: 2rem;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--bg-raised);
    text-align: center;
  }
  .newsletter h3 { margin-bottom: 0.5rem; font-size: 1.1rem; }
  .newsletter p { color: var(--text-muted); font-size: 0.9rem; margin-bottom: 1.25rem; }
  .nl-fields { display: flex; gap: 0.5rem; max-width: 400px; margin: 0 auto; }
  .nl-fields input {
    flex: 1;
    padding: 0.6rem 0.8rem;
    font-size: 0.85rem;
    font-family: var(--font-body);
    border: 1px solid var(--border);
    border-radius: 4px;
    background: var(--bg);
    color: var(--text);
    outline: none;
  }
  .nl-fields input:focus { border-color: var(--accent); }
  .nl-fields button {
    padding: 0.6rem 1.2rem;
    font-size: 0.85rem;
    font-weight: 600;
    font-family: var(--font-body);
    background: var(--accent);
    color: var(--bg);
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  .nl-fields button:hover { opacity: 0.85; }
  @media (max-width: 480px) { .nl-fields { flex-direction: column; } }
</style>
